#!/usr/bin/env bash
set -e

pip install -U pip

globalModules=(
  ansible
  ghp-import
  mercurial_keyring
  pyflakes
  pylint
  virtualenv
)

for i in "${globalModules[@]}"; do
  echo "$i"
  pip install --user "$i"
done
