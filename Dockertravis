FROM clux/debian

ADD ansible.tar dotclux

# hosts...
#[travis]
#travis      desktop_user=clux
#[travis:vars]
#ansible_python_interpreter="/usr/bin/python2"

ENV PATH $PATH:/usr/local/node/bin
RUN cd dotclux && bash -c "pip install -r requirements.txt"
RUN cd dotclux && ./DEPLOY travis
