---
# Package manager interface

- include: arch.yml
  when: ansible_os_family == "Archlinux"
  become: yes
  tags: [arch, os, core]

- include: debian.yml
  when: ansible_os_family == "Debian"
  become: yes
  tags: [debian, os, core]
