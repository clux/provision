---
# OS extras for debian

- include: laptop.yml
  when: ansible_os_family == "Debian"
  become: yes
  tags: [bootstrap]

- include: arch.yml
  when: ansible_os_family == "Archlinux"
  become: yes
  tags: [bootstrap]
