---
# OS extras for debian

- include: apt.yml
  when: ansible_os_family == "Debian"
  become: yes
  tags: [debian, core]

- include: bash.yml
  when: ansible_os_family == "Debian"
  become: yes
  tags: [debian, core]
