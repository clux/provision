---
# OS extras for debian

- include: debian.yml
  when: ansible_os_family == "Debian"
  become: yes
  tags: [debian, core]

- include: bash.yml
  when: ansible_os_family == "Debian"
  become: yes
  tags: [debian, core]
